/**
 * @lozzalingo/ticker
 *
 * Exposes a GET /api/recent-sales endpoint that returns
 * the 5 most recent sales in a standard format for the
 * laurence.computer homepage ticker.
 */

module.exports = require("./server");
