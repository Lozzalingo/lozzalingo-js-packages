const { createSubscriberController } = require('./subscribers.controller');
const { createSubscriberRoutes } = require('./subscribers.routes');
module.exports = { createSubscriberController, createSubscriberRoutes };
