version: '3.8'
services:
  restream:
    build: .
    ports:
      - "1935:1935"
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped
