/**
 * @lozzalingo/ads
 *
 * Cross-site product ad embedding for blog posts.
 * Server: exposes GET /api/products/embed for other sites to fetch products.
 * Client: React components for injecting product ads into article content.
 */

module.exports = require("./server");
